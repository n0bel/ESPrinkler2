{
	"title": "Config",
	"type": "object",
	"properties":
	{
		"host":
		{
			"title": "Host Name",
			"type": "string",
			"propertyOrder": 10
		},
		"ssid":
		{
			"title": "SSID to of the AP connect to",
			"type": "string",
			"propertyOrder": 20
		},
		"password":
		{
			"title": "Password of the AP",
			"type": "string",
			"format": "password",
			"propertyOrder": 30
		},
		"offsetGMT":
		{
			"title": "Timezone Offset (seconds)",
			"type": "integer",
			"default": -18000,
			"propertyOrder": 40
		},
		"timeServer":
		{
			"title": "Timeserver",
			"type": "string",
			"default": "0.pool.ntp.org",
			"propertyOrder": 50
		},
		"resetTime":
		{
			"title": "Reset the device at this time.",
			"description": "00:00 to disable.",
			"type": "string",
			"format": "time",
			"default": "00:00",
			"propertyOrder": 60
		}
	}
}
