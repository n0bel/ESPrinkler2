{
	"title": "Configuration",
	"type": "object",
	"properties":
	{
		"host":
		{
			"title": "Host Name",
			"type": "string",
			"default":"ESPrinkler2",
			"propertyOrder": 10
		},
		"ssid":
		{
			"title": "Your access point SSID (your router).",
			"description": "Blank to disable connecting to an access point.",
			"type": "string",
			"default":"",
			"propertyOrder": 20
		},
		"password":
		{
			"title": "Password of your access point.",
			"type": "string",
			"format": "password",
			"default":"",
			"propertyOrder": 30
		},
		"assid":
		{
			"title": "SSID when acting as an access point.",
			"description": "Blank will create a suitable ssid.",
			"type": "string",
			"default":"",
			"propertyOrder": 40
		},
		"apassword":
		{
			"title": "Password when acting as an access point.",
			"description": "Blank for no password. (Passwords shorter than 8 characters results in no password)",
			"type": "string",
			"format": "password",
			"default":"",
			"propertyOrder": 50
		},
		"offsetGMT":
		{
			"title": "Timezone Offset (seconds)",
			"type": "integer",
			"default": 0,
			"minimum":-86400,
			"maximum":86400,
			"propertyOrder": 60
		},
		"timeServer":
		{
			"title": "NTP Timeserver",
			"description": "Blank to disable NTP.",
			"type": "string",
			"default": "pool.ntp.org",
			"propertyOrder":750
		}
	}
}
